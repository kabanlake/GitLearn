package utility;

public class EuclideanDistance2D implements Distance2D{
    @Override
    public double dist(Point2D p1, Point2D p2) {
        return Math.sqrt(Math.pow(p1.getX() - p2.getX(), 2) + Math.pow(p1.getY() - p2.getY(), 2));
    }
}
